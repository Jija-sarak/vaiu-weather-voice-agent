import type { ChatContext } from '../../src/llm/chat_context.js';
import type { VoiceOptions } from '../../src/voice/agent_session.js';
import type { AgentEvent } from '../../src/voice/events.js';
export interface SessionReport {
    jobId: string;
    roomId: string;
    room: string;
    options: VoiceOptions;
    events: AgentEvent[];
    chatHistory: ChatContext;
    enableUserDataTraining: boolean;
    timestamp: number;
}
export interface SessionReportOptions {
    jobId: string;
    roomId: string;
    room: string;
    options: VoiceOptions;
    events: AgentEvent[];
    chatHistory: ChatContext;
    enableUserDataTraining?: boolean;
    timestamp?: number;
}
export declare function createSessionReport(opts: SessionReportOptions): SessionReport;
export declare function sessionReportToJSON(report: SessionReport): Record<string, unknown>;
//# sourceMappingURL=report.d.ts.map